/**
 * WeCar Dashboard — JS
 *
 * Funcionalidad frontend para el dashboard de wp-admin.
 */
(function ($) {
    'use strict';

    // Tooltip para las barras de progreso (futuro)
    $(function () {
        console.log('WeCar Dashboard loaded');
    });

})(jQuery);
